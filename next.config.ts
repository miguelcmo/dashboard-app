import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: { // This is only for experimental purposes PPR
  //   ppr: 'incremental',
  // }
};

export default nextConfig;
